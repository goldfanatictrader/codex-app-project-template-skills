interface:
  display_name: "API Contract Integration"
  short_description: "Check API contracts and compatibility"
  default_prompt: "Use $api-contract-integration to review this API change for schema consistency, consumer impact, and compatibility risk."

policy:
  allow_implicit_invocation: false
