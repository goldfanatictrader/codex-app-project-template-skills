interface:
  display_name: "Project Developer"
  short_description: "Combine memory, coding, and verification"
  default_prompt: "Use $project-developer to read project memory, implement the change, verify it, and update the session history."

policy:
  allow_implicit_invocation: false
