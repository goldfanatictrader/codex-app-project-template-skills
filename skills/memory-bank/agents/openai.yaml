interface:
  display_name: "Memory Bank"
  short_description: "Bootstrap, read, and update project memory"
  default_prompt: "Use $memory-bank to bootstrap project memory if needed, read it first, and update it before the session ends."

policy:
  allow_implicit_invocation: true
