interface:
  display_name: "Memory Bank"
  short_description: "Read and update project memory"
  default_prompt: "Use $memory-bank to read the project memory first and update it before the session ends."

policy:
  allow_implicit_invocation: false
